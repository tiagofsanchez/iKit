/** @jsx jsx */
import { jsx } from "theme-ui"
import Layout from "../components/layout"

const Search = () => (
  <Layout withHero={true}>
    <div sx={{ variant: `layout.main` }}>search</div>
  </Layout>
)

export default Search
